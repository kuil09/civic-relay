# Contributing

## 시작 전

1. `README.md`, `AGENTS.md`, 관련 이슈를 읽는다.
2. 기술 결정이 필요하면 `docs/adr/`에 ADR을 추가하거나 기존 결정을 따른다.
3. 실제 개인 사례와 연락처를 fixture나 예제에 사용하지 않는다.
4. 하나의 변경이 어떤 PRD 요구사항과 수용 기준을 다루는지 적는다.

## 개발 원칙

- Node.js 22 이상, ESM, 런타임 의존성 0을 기본으로 한다.
- 파일 기반 상태와 사람이 읽을 수 있는 Markdown을 유지한다.
- 외부 도구 연동은 코어와 분리된 어댑터로 구현한다.
- 실패를 숨기거나 암묵적으로 통과시키지 않는다.
- 법령·조직·연락처를 코드에 최신 상수로 박아 넣지 않는다.
- 테스트는 실제 이메일이나 외부 시스템 변경을 수행하지 않는다.

## 변경 절차

```bash
npm test
npm run check
npm run validate:example
```

PR에는 다음을 포함한다.

- 해결하거나 대응하는 이슈
- 변경된 계약이나 데이터 모델
- 테스트와 재현 방법
- 남아 있는 불확실성
- 외부 자료의 확인 시점

## 스킬 추가

새 `SKILL.md`는 `docs/skill-contract.md`의 필수 섹션을 모두 포함해야 한다. 스킬은 자신이 읽고 쓰는 파일과 `case.json` 필드를 명시해야 하며 승인 상태를 임의로 변경할 수 없다.

## 보안 문제

비밀정보나 개인정보 유출은 공개 이슈에 원문을 붙이지 말고 `SECURITY.md`의 비공개 보고 절차를 사용한다.
