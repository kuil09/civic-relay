# Civic Relay Agent Contract

이 문서는 저장소에서 작업하는 인간과 AI 에이전트가 따라야 할 단일 운영 계약이다. 도구별 지침은 이 문서를 참조해야 하며 핵심 규칙을 별도로 재정의하지 않는다.

## 1. 임무

시민의 원문을 보존하면서 문제를 검증 가능한 정책 의제로 만들고, 법·권한·이해관계·대안·반론·수신자·전달 상태를 재현 가능한 파일로 남긴다.

## 2. 절대 규칙

1. 사용자의 최초 진술은 `00-intake.md`와 `case.json.original_statement`에 그대로 보존한다.
2. 관찰, 사실, 추론, 가치 판단, 제안을 구분한다.
3. 법적 사실은 공식 법령 원문이나 공식 해석을 확인하기 전까지 확정하지 않는다.
4. 현직자, 위원회, 부처 조직, 연락처는 저장소의 오래된 예시를 최신 정보로 사용하지 않는다.
5. 모든 수신자는 선정 이유와 공식 연락 경로의 출처를 가져야 한다.
6. 현행 유지안을 포함한 실질 대안 최소 4개와 강한 반론 최소 3개를 검토한다.
7. 사용자의 최초 해결안을 폐기하는 결론도 허용한다.
8. 사람의 명시적 승인 없이 승인 레코드를 만들거나 메일 도구를 호출하지 않는다.
9. 다른 수신자의 주소를 노출하지 않는다.
10. 동일 사례·문서 해시·수신자 조합의 반복 발송을 차단한다.
11. 존재하지 않는 지지자·단체·서명·경력·근거를 만들지 않는다.
12. 공식 공개 업무 채널만 사용한다.
13. 오류나 조사 실패를 사실로 메우지 않는다. `unresolved` 상태와 다음 조사 항목으로 남긴다.
14. 실제 이메일, 전화번호, 주소, API 키, OAuth 토큰, 회신 원문은 공개 저장소에 커밋하지 않는다.

## 3. 작업 순서

### 3.1 접수

관련 스킬: `issue-intake`, `problem-framing`

- 원문과 원하는 변화의 수준을 기록한다.
- 문제 현상과 선호하는 해결안을 분리한다.
- 관할, 장소, 시간, 영향을 받는 집단을 추출한다.
- 확인되지 않은 원인은 가설로 남긴다.

산출물: `00-intake.md`, `01-issue-brief.md`, `case.json`

### 3.2 근거 조사

관련 스킬: `evidence-research`

자료 우선순위:

1. 공식 법령
2. 국회·상임위원회 공식 자료
3. 중앙부처·지자체·공공기관 공식 자료
4. 국가 통계·공공 연구기관
5. 학술 연구·전문단체
6. 신뢰 가능한 언론
7. 인터뷰·커뮤니티

모든 외부 사실에는 출처, 발행일, 확인일, 적용 시점, 신뢰 수준을 기록한다. 충돌 자료를 제거하지 않는다.

산출물: `02-evidence-dossier.md`, `claims`, `sources`

### 3.3 법과 권한

관련 스킬: `law-policy-mapping`, `authority-routing`, `committee-routing`

- 법률, 대통령령, 부령, 조례, 행정지침, 관리규약, 민간 운영방침을 구분한다.
- 입법, 제정, 집행, 감독, 시설 운영 권한을 구분한다.
- 한 기관으로 해결할 수 없는 경우 공동 소관을 명시한다.
- 의원 개인 이름보다 역할과 공식 검증 경로를 먼저 정의한다.

산출물: `03-law-and-authority-map.md`

### 3.4 이해관계·대안·반론

관련 스킬: `stakeholder-analysis`, `option-design`, `counterargument-redteam`

- 직접 수혜자, 직접 부담자, 권리 침해 가능 집단, 비용 부담자, 집행 주체를 식별한다.
- 현행 유지 및 집행 개선, 행정·운영 개선, 제한적 시범, 입법안 등 최소 4개 대안을 비교한다.
- 가장 강한 반론과 악용·확대·집행 실패·비용 전가 가능성을 검토한다.

산출물: `04-stakeholder-map.md`, `05-options-memo.md`, `06-counterarguments.md`

### 3.5 정책 문서

관련 스킬: `proposal-writing`

문서에는 문제, 확인된 사실, 현행 제도, 권한 구조, 이해관계, 대안, 반론, 요청 사항, 출처가 포함되어야 한다. 수신자가 할 수 없는 조치를 요청하지 않는다.

산출물: `07-policy-proposal.md`, `build/one-page-summary.md`, `build/evidence-appendix.md`

### 3.6 수신자

관련 스킬: `recipient-resolution`

- 직접, 공동, 참고·감독 등급을 부여한다.
- 기관·역할·선정 이유·기대 조치·공식 채널·출처·검증 시각을 기록한다.
- 현직과 연락 경로는 수신자 확정 시점과 발송 직전에 다시 확인한다.
- 재검증에 실패한 대상은 `selected=false` 또는 `blocked`로 둔다.

산출물: `08-recipient-matrix.csv`, `case.json.recipients`

### 3.7 전달 준비와 승인

관련 스킬: `delivery-packaging`

- 핵심 문서는 모든 수신자에게 동일한 버전을 사용한다.
- 제목, 호칭, 소관 이유, 요청 가능한 조치만 수신자별로 조정한다.
- 실제로 선택한 기관 범주만 공동 전달 고지에 포함한다.
- 리뷰 문서에서 사실 오류, 법적 불확실성, 수신자, 첨부물, 배포 범위를 한 번에 확인한다.

승인 단계: `problem`, `evidence`, `policy`, `recipients`, `document`, `dispatch`

AI는 승인자가 될 수 없다. 승인 뒤 파일이나 구조화 데이터가 바뀌면 해당 승인은 만료된다.

### 3.8 발송

관련 스킬: `mail-dispatch`

- 기본 모드는 `draft`다.
- `send`에는 유효한 6단계 승인, 최신 수신자 검증, 중복 검사, 명시적인 외부 어댑터가 필요하다.
- 복수 수신자는 개별 메시지로 처리한다.
- 필터 회피용 무작위 문구 변형을 하지 않는다.
- 성공·실패·반송·대체 경로를 발송 명세에 기록한다.

산출물: `09-cover-emails/`, `10-dispatch-manifest.json`, `build/outbox/`

### 3.9 회신

관련 스킬: `response-tracking`

- 원문과 AI 요약을 분리한다.
- 접수, 소관 아님, 이관, 자료 요청, 반대, 검토 약속, 면담 제안, 불확실로 분류한다.
- 답변이 없음을 거부로 해석하지 않는다.
- 후속 메시지도 별도 사용자 승인을 요구한다.

산출물: `11-responses/`, `12-follow-up.md`

## 4. 파일 규칙

- 각 사례의 단일 구조화 상태는 `case.json`이다.
- Markdown은 사람의 검토와 근거 설명을 담당한다.
- 구조화 데이터와 문서가 충돌하면 작업을 중단하고 불일치를 보고한다.
- 생성물은 덮어쓰기 전에 기존 사용자 편집을 감지한다.
- 시간은 ISO 8601 UTC로 기록한다.
- 식별자는 사례 내부에서 안정적으로 유지한다.
- 공개 예제에는 실제 개인 연락처를 넣지 않는다.

## 5. 현재성 게이트

다음은 `send` 직전에 공식 출처로 재검증한다.

- 현직 의원과 상임위원회 소속
- 위원장·간사·소위원회 직책
- 부처 명칭과 조직
- 공식 업무 연락 경로
- 법령의 현행 조문과 시행일
- 진행 중 입법안의 상태

기본 유효기간은 24시간이며 관할 설정으로 줄일 수 있다. 검증 실패는 발송 차단 사유다.

## 6. 완료 조건

작업은 문서가 그럴듯해 보일 때 끝나지 않는다.

- 저장소 검사가 통과한다.
- 사례 검증기가 오류 없이 끝난다.
- 핵심 사실이 모두 출처 또는 사용자 관찰 표시를 갖는다.
- 권한 경계와 수신자 선정 이유를 추적할 수 있다.
- 현행 유지안을 포함한 대안과 강한 반론이 있다.
- 공개본에 개인정보와 비밀정보가 없다.
- 외부 변경은 명시적 인간 승인 없이는 0건이다.

## 7. 도구가 없을 때

웹 검색이 없으면 조사 계획과 공식 자료 후보를 생성하되 사실을 확정하지 않는다. 메일 도구가 없으면 검토 가능한 초안과 `dispatch-request.json`을 만든다. PDF 도구가 없으면 Markdown을 정본으로 유지한다.
